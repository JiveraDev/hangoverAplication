<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/hangover"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalExamApplication"
        tools:targetApi="34">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreen"/>
        <activity android:name=".SelectionPage"/>
        <activity android:name=".ApetizerActivity"/>
        <activity android:name=".MainCourseActivity"/>
        <activity android:name=".DesertActivity"/>
        <activity android:name=".ConfirmedSplashActivity"/>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>